{
  "name": "react-dates-esm-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install:packed": "node install-packed.mjs",
    "test": "node verify.mjs",
    "test:packed": "npm run install:packed && npm test"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
