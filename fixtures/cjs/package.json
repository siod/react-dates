{
  "name": "react-dates-cjs-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "install:packed": "node install-packed.cjs",
    "test": "node verify.cjs",
    "test:packed": "npm run install:packed && npm test"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
